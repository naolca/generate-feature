class <%= name %>Controller {
    async create(req, res) {
      // Logic for creating a new <%= name.toLowerCase() %>
    }
  
    async findAll(req, res) {
      // Logic for fetching all <%= name.toLowerCase() %>s
    }
  
    async findOne(req, res) {
      // Logic for fetching a single <%= name.toLowerCase() %> by ID
    }
  
    async update(req, res) {
      // Logic for updating an existing <%= name.toLowerCase() %>
    }
  
    async delete(req, res) {
      // Logic for deleting an existing <%= name.toLowerCase() %>
    }
  }
  
  module.exports = <%= name %>Controller;
  